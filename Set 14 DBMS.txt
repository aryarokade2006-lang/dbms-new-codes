Set 14
Create table employees and department and solve the following queries

1.Write a query to display the JOB,MGR,SAL,COMM,DNAME of employees whose JOB is SALESMAN.
2.The HR department needs a report of employees in Pune.Display the ENAME,job,DEPTNO and DNAME
for all employees who work in Pune.
3.Display the Ename,Dname of all the employees,Also display those department name which do not have
any employees working
4.Display the Ename,Dname of all the employees,Also display those departemnt name which do not have
any employees working
5.Display the EMPNO,ENAME,LOC of those employees who are working as CLERK

CREATE TABLE dept14(
department_id NUMBER(4),

department_name VARCHAR2(30) NOT NULL,

manager_id NUMBER(6),

location_id NUMBER(4),

CONSTRAINT pk_dept14
PRIMARY KEY(department_id)
);

CREATE TABLE emp14(
employee_id NUMBER(6),

first_name VARCHAR2(20),

last_name VARCHAR2(25) NOT NULL,

email VARCHAR2(25) NOT NULL,

phone_number VARCHAR2(20),

hire_date DATE NOT NULL,

job_id VARCHAR2(10) NOT NULL,

salary NUMBER(8,2),

commission_pct NUMBER(2,2),

manager_id NUMBER(6),

department_id NUMBER(4),

CONSTRAINT pk_emp14
PRIMARY KEY(employee_id)
);

ALTER TABLE emp14
ADD CONSTRAINT fk_emp14_dept14
FOREIGN KEY(department_id)
REFERENCES dept14(department_id);

INSERT INTO dept14 VALUES(10,'ACCOUNTING',201,100);

INSERT INTO dept14 VALUES(20,'RESEARCH',202,200);

INSERT INTO dept14 VALUES(30,'SALES',203,300);

INSERT INTO dept14 VALUES(40,'OPERATIONS',204,400);

INSERT INTO dept14 VALUES(50,'HR',205,500);

INSERT INTO emp14
VALUES(
101,
'Riddhi',
'Bacchewar',
'RMAIL',
'9999999999',
'10-JAN-2024',
'SALESMAN',
5000,
0.10,
201,
10
);

INSERT INTO emp14
VALUES(
102,
'Amit',
'Sharma',
'AMAIL',
'8888888888',
'12-FEB-2024',
'CLERK',
2500,
0.20,
202,
20
);

INSERT INTO emp14
VALUES(
103,
'Neha',
'Patil',
'NMAIL',
'7777777777',
'15-MAR-2024',
'MANAGER',
3500,
0.15,
203,
30
);

INSERT INTO emp14
VALUES(
104,
'Pooja',
'More',
'PMAIL',
'6666666666',
'20-APR-2024',
'CLERK',
1500,
0.25,
204,
20
);

COMMIT;

SELECT
job_id,
manager_id,
salary,
commission_pct,
department_name
FROM emp14 e
JOIN dept14 d
ON e.department_id = d.department_id
WHERE job_id='SALESMAN';

SELECT
first_name,
job_id,
e.department_id,
department_name
FROM emp14 e
JOIN dept14 d
ON e.department_id = d.department_id
WHERE location_id = 200;

SELECT
first_name,
department_name
FROM emp14 e
RIGHT OUTER JOIN dept14 d
ON e.department_id = d.department_id;

SELECT
first_name,
department_name
FROM emp14 e
RIGHT OUTER JOIN dept14 d
ON e.department_id = d.department_id;

SELECT
employee_id,
first_name,
location_id
FROM emp14 e
JOIN dept14 d
ON e.department_id = d.department_id
WHERE job_id='CLERK';